<div></div>

===
reactJS-Redux-Mastering-Web-Apps

git clone https://github.com/lcmpbarga/reactJS-Redux-Mastering-Web-Apps.git

===branchs===
git checkout -b countdownChamp
git push origin countdownChamp

git checkout master
git merge countdownChamp

git checkout  countdownChamp
=
git checkout -b reminderPro
git push origin reminderPro

git checkout master
git merge reminderPro

git checkout  reminderPro
=
git checkout -b  goalCoach
git push origin reminderPro

git checkout master
git merge reminderPro

git checkout  reminderPro

===react===
npm install -g create-react-app
npx create-react-app reactJS-Redux
create-react-app countDownChamp

npm install --save react react-dom
npm install --save react-bootstrap

npm install redux react-redux --save
npm install moment --save
npm install sfcookies

npm install redux react-redux react-router firebase --save
npm install react-router@3 --save


===terminal_util===
sudo su (for npm commands)
rm -r /home/computer/projects/reactjs
